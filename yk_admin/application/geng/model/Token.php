<?php
namespace app\geng\model;
use think\Model;

class Token extends Model
{
	// function takeid($token){
	// 	$id = db('token')->where(['token'=>$token])->field('user_id')->find();
	// 	// mp($id);
	// 	return $id;	
	// }
	
}